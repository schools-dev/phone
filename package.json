{
  "name": "phone",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "echo 'Static site - no build needed' && exit 0",
    "build": "echo 'Static site - no build needed' && exit 0"
  }
}
